# Configuration parameters for message_translator

# Frame configuration
map_frame: "map"                    # Base coordinate frame
target_frame: "base_link"           # Target frame for TF transforms
tf_prefix: ""                       # Prefix for TF frame names
prefix_global_frame: false          # Whether to apply prefix to global frame
